<%= simple_form_for(@reiteo, html: { class: "input-group"}) do |f| %>
  <%= f.error_notification %>

    <%= f.input :description, as: :text, input_html: { rows: "3"} %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
